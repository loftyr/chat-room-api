version: "3.9"

services:
  app:
    build: .
    container_name: chatapi
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:80"
    networks:
      - app_network

networks:
  app_network:

volumes:
  postgres_data:
